
name: create a json file when a new issue is opened
on:
  issues:
    types:
      - opened
      - labeled
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        uses: actions/checkout@v3
      - name: Json Generate
        run: pwd