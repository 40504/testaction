{
  "name": "fix",
  "version": "1.0.0",
  "description": "fix",
  "scripts": {
    "push": "git pull && git commit -a -m 'update' && git add . -A && git push"
  },
  "dependencies": {
    "axios": "^1.5.0"
  }
}
